import { View } from 'react-native';

/** Placeholder tab — press intercepted in _layout.tsx to open /generate-qr */
export default function QRPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: '#FFFFFF' }} />;
}
