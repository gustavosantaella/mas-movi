/**
 * Design System — barrel export.
 *
 * Usage:
 *   import { Colors, Gradients, Spacing, Typography } from '@/theme';
 */

import '@/global.css';

export { Colors, Gradients, type ThemeColor } from './colors';
export { Spacing, BorderRadius, BottomTabInset, MaxContentWidth } from './spacing';
export { Fonts, Typography } from './typography';
